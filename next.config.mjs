/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com','www.shutterstock.com'], // Add any other domains you need here
  },
};

export default nextConfig;
